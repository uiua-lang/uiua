# This is meant to be run in the Uiua window
# Experimental!
Turn ← ⬚0⊏⊸(⌊×⊃-₁(⌵/ℂ°⍉˙⊞⊟-⊸¬÷⟜(⇡+1)×2)⧻)
↙2000 Apple
≡⬚0⊂⟜(
  ×τ÷×8 24°⊏
  wait≡pool(
    ⊙(≡⊂⟜⇌ ≡(Turn))
    voxels!(°⊸Fog Black °⊸Camera[0.3°∠])
    ↘¯⟜↘50
  )
)
Bad
